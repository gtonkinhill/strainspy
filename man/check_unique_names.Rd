% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{check_unique_names}
\alias{check_unique_names}
\title{Stop if sample or feature names are not unique}
\usage{
check_unique_names(x, what, clean_names = FALSE)
}
\arguments{
\item{x}{Character vector of names to check.}

\item{what}{Character. What the names identify, used in the error message
(for example \code{"sample"} or \code{"feature"}).}

\item{clean_names}{Logical. Whether the caller applied basename cleaning; used
only to tailor the suggested remedy.}
}
\value{
\code{x}, invisibly, if every name is unique.
}
\description{
\code{read_sylph()}, \code{read_metaphlan()} and \code{read_sourmash()} all reduce
\code{Sample_file} to a bare basename when \code{clean_names = TRUE}. Two input paths
that differ only by directory (\code{runA/S1.fastq.gz} and \code{runB/S1.fastq.gz})
therefore collapse to the same name. Duplicated names are not rejected by
\code{SummarizedExperiment}, and every downstream \code{match()} on sample or feature
names would then silently attach the wrong metadata to the wrong column, so
fail here instead.
}
