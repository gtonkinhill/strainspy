% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{failed_fit_index}
\alias{failed_fit_index}
\title{Identify per-feature model fits that failed}
\usage{
failed_fit_index(results)
}
\arguments{
\item{results}{A list of per-feature fit results, one element per feature,
in the same order as \code{rowData(se)}.}
}
\value{
An increasing integer vector of positions in \code{results} that should be
dropped, or \code{integer(0)} if every fit succeeded.
}
\description{
The fitting backends signal failure in two different ways. \code{glmObFit()},
\code{glmQBFit()} and \code{abundanceFit()} return \code{NULL} for a feature that could not
be fitted, so the failure shows up as a zero-length entry. Both \code{glmZiBFit()}
backends instead return a fully-populated list whose coefficient matrices are
\code{NA} and whose \code{log_likelihood} is \code{NA}, so the zero-length test never fires
there and the \code{NA} log-likelihood is the only signal.
}
\details{
Detecting only one of the two leaves failed fits in the results, and applying
the \code{log_likelihood} test blindly is worse: \code{glmQBFit()} does not report a
log-likelihood at all, so every successful fit would look like a failure.
This helper handles both protocols by treating a log-likelihood that is
absent from \emph{every} result as "not reported" rather than "all failed".

It deliberately uses \code{vapply()} rather than \code{unlist()}. \code{unlist()} silently
drops the \code{NULL} log-likelihood of a zero-length entry, which shortens the
vector and shifts every later index, so the returned positions would no
longer line up with \code{results} or with \code{rowData()}.
}
