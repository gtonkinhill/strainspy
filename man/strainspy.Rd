% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strainspy.R
\name{strainspy}
\alias{strainspy}
\title{Strainspy Wrapper}
\usage{
strainspy(
  meta_path,
  sylph_path,
  design_formula,
  coef,
  min_nonzero = NULL,
  taxonomy_path = NULL,
  output_path = NULL,
  alpha = 0.05,
  p_adjust_method = "holm",
  model_method = "glmmTMB",
  scale_continuous = TRUE,
  nthreads = 2,
  return_vcov = FALSE,
  attach_taxonomy = TRUE,
  overwrite = TRUE
)
}
\arguments{
\item{meta_path}{Path to the metadata file (CSV format).}

\item{sylph_path}{Path to the sylph output file (TSV format).}

\item{design_formula}{One-sided formula (or string coercible to formula) for
the design matrix, e.g. \code{~ Condition + Age + (1|Subject)}.}

\item{coef}{Coefficient index to report in \code{top_hits()}. This determines
which design term is tested.}

\item{min_nonzero}{Minimum number of samples for strain presence filtering.
If \code{NULL} (default), this is set to \code{ceiling(0.10 * n_samples)}.}

\item{taxonomy_path}{Optional path to a taxonomy file (TSV format).
If provided, taxonomy columns are appended to top hits.}

\item{output_path}{Optional path to save top hits as CSV. If \code{NULL}, no file is written.}

\item{alpha}{Significance threshold used by \code{top_hits()}.}

\item{p_adjust_method}{Multiple-testing correction method for \code{top_hits()}.}

\item{model_method}{Backend used by \code{glmZiBFit()}: \code{"glmmTMB"} or \code{"gamlss"}.}

\item{scale_continuous}{If \code{TRUE}, continuous metadata covariates are z-scored
by \code{glmZiBFit()}.}

\item{nthreads}{Number of threads for parallel processing (default: all available cores).}

\item{return_vcov}{If \code{TRUE}, include per-feature variance-covariance matrices in fit.}

\item{attach_taxonomy}{If \code{TRUE} and \code{taxonomy_path} is provided, append taxonomy columns to top hits.}

\item{overwrite}{If \code{FALSE}, abort when \code{output_path} already exists.}
}
\value{
A data frame of top hits. If \code{taxonomy_path} is supplied and
\code{attach_taxonomy = TRUE}, taxonomy columns are appended.
}
\description{
A wrapper to run a full strainspy workflow with explicit input validation.
}
\examples{
\donttest{
meta_path <- system.file("extdata", "example_metadata.csv.gz", 
package = "strainspy")
sylph_path <- system.file("extdata", "example_sylph_profile.tsv.gz", 
package = "strainspy")
taxonomy_path <- system.file("extdata", "example_taxonomy.tsv.gz", 
package = "strainspy")

# Decision 1: choose predictors for the design formula
# Decision 2: choose which coefficient index to report in top_hits()
# For ~ Case_status + Age_at_collection, coef = 2 corresponds to Case_status

out <- strainspy(
  meta_path = meta_path,
  sylph_path = sylph_path,
  design_formula = "~ Case_status + Age_at_collection",
  coef = 2,
  taxonomy_path = taxonomy_path,
  output_path = tempfile(fileext = ".csv"),
  nthreads = 2,
  alpha = 1
)
head(out)
}
}
